version: '2'
services:
    adap_assessment-elasticsearch:
        container_name: adap_assessment-elasticsearch
        image: elasticsearch:1.7.5
        # volumes:
        #     - ~/volumes/jhipster/adap_assessment/elasticsearch/:/usr/share/elasticsearch/data/
        ports:
            - 9200:9200
            - 9300:9300
